#!/usr/bin/env python

from setuptools import setup, find_packages


setup(
    name='BioTorch',
    version='0.0.1',
    description='',
    author='',
    author_email='',
    url='',
    packages=find_packages(exclude=['tests', '.cache', '.venv', '.git', 'dist']),
    scripts=[],
    install_requires=[

    ]
)
