# Experiment Information
experiment:
  name: "new_experiment_CIFAR10_Benchmark_weight_mirroring_resnet20_f"
  output_dir: "trained_models"

# Dataset
dataset:
  name: "cifar10_benchmark"

# Model
model:
  architecture: "resnet20"
  mode: "weight_mirroring"
  pretrained: false
  # checkpoint:

# Training Parameters
training:
  hyperparameters:
    epochs: 120
    batch_size: 128
    target_size: 32

  optimizer:
    type: "SGD"
    lr: 0.0001
    weight_decay: 0.00001
    momentum: 0.9

  lr_scheduler:
    type: "multistep_lr"
    gamma: 0.1
    milestones: [70, 100]

  metrics:
    top_k: 5
    display_iterations: 500

  loss_function:
    name: "cross_entropy"


# Infrastructure
infrastructure:
  num_gpus: 2
