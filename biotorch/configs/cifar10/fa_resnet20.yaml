# Experiment Information
experiment:
  name: "CIFAR10_Benchmark_FA_resnet20"
  output_dir: "trained_models"

# Dataset
data:
  dataset: "cifar10_benchmark"
  dataset_path: null

# Model
model:
  architecture: "resnet20"
  mode: "fa"
  pretrained: false
  # checkpoint:

# Training Parameters
training:
  hyperparameters:
    epochs: 300
    batch_size: 128
    target_size: 32

  optimizer:
    type: "SGD"
    lr: 0.1
    weight_decay: 0.0001
    momentum: 0.9

  lr_scheduler:
    type: "multistep_lr"
    gamma: 0.1
    milestones: [100, 200, 250]

  metrics:
    top_k: 5
    display_iterations: 500

  loss_function:
    name: "cross_entropy"


# Infrastructure
infrastructure:
  gpus: 2
